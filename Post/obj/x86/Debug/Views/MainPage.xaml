<Page x:ConnectionId='1' x:Class="Post.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Post.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Post.ViewModels"
      xmlns:data="using:Post.Models"
      xmlns:conv="using:Post.Converters" 
      mc:Ignorable="d">

    <Page.Resources>
        <conv:DateFormatConverter x:ConnectionId='2' x:Name="DateToStringFormatConverter"/>
    </Page.Resources>
    
    <Page.DataContext>
        <vm:MainPageViewModel x:ConnectionId='3' x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>


        <RelativePanel Grid.Column="0">

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:ConnectionId='16' x:Name="AdaptiveVisualStateGroup">
                    <VisualState x:ConnectionId='17' x:Name="VisualStateNarrow">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <!--  TODO: change properties for narrow view  -->
                            <Setter Target="stateTextBox.Text" Value="Narrow Visual State" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:ConnectionId='18' x:Name="VisualStateNormal">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <!--  TODO: change properties for normal view  -->
                            <Setter Target="stateTextBox.Text" Value="Normal Visual State" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:ConnectionId='19' x:Name="VisualStateWide">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <!--  TODO: change properties for wide view  -->
                            <Setter Target="stateTextBox.Text" Value="Wide Visual State" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <controls:PageHeader x:ConnectionId='20' x:Name="pageHeader"
                             RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True"
                             Text="Post it - Wall">

                <!--  secondary commands  -->
                <controls:PageHeader.SecondaryCommands>
                    <AppBarButton x:ConnectionId='21'                                         Label="Settings" />
                    <AppBarButton x:ConnectionId='22'                                      Label="About" />
                </controls:PageHeader.SecondaryCommands>

            </controls:PageHeader>
        </RelativePanel>



        <!--<RelativePanel Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">-->
        <RelativePanel Grid.Row="1" HorizontalAlignment="Center">
            <GridView x:ConnectionId='14' MinHeight="300" MinWidth="200"                                                  CanDragItems="True" AllowDrop="True" CanReorderItems="True">
                <GridView.ItemTemplate >
                    <DataTemplate                                >
                        <ListView x:ConnectionId='15' ItemsSource="{Binding NoteCollection}" 
                                  Height="300" Header="Grab me" 
                                  Margin="10,0,0,0" 
                                  CanDragItems="True" 
                                  AllowDrop="True"
                                                                                          
                                                                           
                                                                                            
                                                                > 
                            <!--<ListView ItemsSource="{Binding NoteCollection}" Height="300" CanDragItems="True" AllowDrop="True" CanReorderItems="True" Header="Collection"  Header="Grab me">-->
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Padding" Value="0,0,0,-170"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="MinHeight" Value="20" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate                       >
                                    <StackPanel Height="202" Width="200" Margin="10,0,10,0" BorderThickness="2" BorderBrush="#FF320000" RenderTransformOrigin="0.5,0.5">
                                        <StackPanel.Background>
                                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                <GradientStop Color="#FF935920" Offset="0"/>
                                                <GradientStop Color="#FFC59564" Offset="1"/>
                                            </LinearGradientBrush>
                                        </StackPanel.Background>
                                        <TextBlock Name="header" Text="{Binding header}" Margin="5,5,5,5" Height="30" TextWrapping="NoWrap" HorizontalAlignment="Center" FontSize="20" FontWeight="ExtraBold" Foreground="#FFD6B900" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
                                        <TextBlock Name="content" Text="{Binding content}" Margin="5,0,5,5" Height="135" TextWrapping="Wrap" FontSize="16" FontStyle="Italic" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="header"/>
                                        <TextBlock Name="date" Text="{Binding date, Converter={StaticResource DateToStringFormatConverter}}" Margin="5,0,5,0" Height="20" TextWrapping="NoWrap" VerticalAlignment="Bottom" Foreground="Black" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignRightWithPanel="True" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </RelativePanel>
        <Grid Grid.Row="1" Background="#B2000000">
            <RelativePanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel x:ConnectionId='8' Name="createNote" Height="200" Width="200" Margin="10,5,10,5">
                    <StackPanel.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF935920" Offset="0"/>
                            <GradientStop Color="#FFC59564" Offset="1"/>
                        </LinearGradientBrush>
                    </StackPanel.Background>
                    <TextBox x:ConnectionId='11' ToolTipService.ToolTip="Title" MaxLength="14" Name="header" Margin="5,5,5,5" Height="35" TextWrapping="NoWrap" HorizontalAlignment="Stretch" TextAlignment="Center" FontSize="20" FontWeight="ExtraBold" Foreground="#FFD6B900" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
                    <TextBox x:ConnectionId='12' ToolTipService.ToolTip="Your task" MaxLength="120" Name="content" Margin="5,0,5,4" Height="120" TextWrapping="Wrap" FontSize="16" FontStyle="Italic" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="header"/>
                    <TextBox x:ConnectionId='13' ToolTipService.ToolTip="Date (dd.mm.yyyy)" MaxLength="10" Name="date" Margin="5,0,5,0" Height="15" TextWrapping="NoWrap" VerticalAlignment="Bottom" Foreground="Black" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignRightWithPanel="True" HorizontalAlignment="Right" TextAlignment="Right"/>
                </StackPanel>
                <Button x:ConnectionId='9' x:Name="okButton"
                        Margin="0,5,0,0"
                        Content="Ok"  
                        Width="80"
                        RelativePanel.Below="createNote" RelativePanel.AlignRightWithPanel="True"/>
                <Button x:ConnectionId='10' x:Name="cancelButton"
                        Margin="0,5,0,0"
                        Content="Cancel"  
                        Width="80"
                        RelativePanel.Below="createNote" RelativePanel.AlignLeftWithPanel="True"/>
            </RelativePanel>
        </Grid>
        <!--</RelativePanel>-->
        <RelativePanel VerticalAlignment="Stretch"
                       HorizontalAlignment="Stretch"
                       Grid.Row="2">

            <Button x:ConnectionId='4' x:Name="debugButton"
                                                        
                    Content="Debug"  
                    Width="200"
                    VerticalAlignment="Stretch" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" />

            <Button x:ConnectionId='5' x:Name="submitButton"
                                                           
                    Content="New Task"  
                    Width="200"
                    VerticalAlignment="Stretch" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignRightWithPanel="True" />

            <UserControl x:ConnectionId='6' x:Name="removeControl"
                         Width="100" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch" 
                         RelativePanel.AlignTopWithPanel="True" 
                         RelativePanel.AlignBottomWithPanel="True" 
                         RelativePanel.AlignLeftWithPanel="True" 
                         RelativePanel.AlignRightWithPanel="True"
                         AllowDrop="True"
                                                                
                                                     
                         Visibility="{Binding isRemoveVisible,Mode=Twoway}">
                <TextBlock x:ConnectionId='7' x:Name="textBlock" HorizontalAlignment="Stretch" Text="(X)" VerticalAlignment="Stretch" FontSize="36" FontWeight="Bold" TextAlignment="Center"/>
            </UserControl>

        </RelativePanel>
    </Grid>
</Page>


